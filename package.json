{
  "name": "calendar-proxy",
  "version": "1.0.0",
  "description": "Proxy to create Google Calendar events (for VAPI)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "googleapis": "^124.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
